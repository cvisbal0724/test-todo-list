import { StyleSheet } from 'react-native';
import { $primary, $colorGray } from '../../domain/constants/Colors';

export const LayoutStyles= StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        // alignItems: 'center',
        // justifyContent: 'center',
    },
    content: {
        // flex: 1,
        // alignItems: 'center',
        flex: 1,
        backgroundColor: '#f7f7f7',
        padding: 16,
      },
});